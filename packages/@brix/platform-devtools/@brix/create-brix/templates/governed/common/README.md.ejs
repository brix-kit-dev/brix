# <%= moduleId %>

Generated by `@brix-sdk/create-brix` Runtime Shell v3.0.10 Phase 7 governed scaffolding.

## Architecture Baseline

- Root: runtime-shell 3.0.10
- Frontend: 1.1
- Super Admin: 3.0
- Multi-tenant: 4.0

## Generated Boundary

- Kind: `<%= kind %>`
- Owner: `<%= owner %>`
- Descriptor: `<%= descriptorPath %>`
- Runtime: `<%= runtimeVersion %>` with range `<%= runtimeRange %>`

## Verification

Run the repository Phase 7 smoke test before moving this generated module out of migration inventory:

```bash
corepack pnpm --filter @brix-sdk/create-brix build
corepack pnpm --filter @brix-sdk/create-brix test:phase7
```

Implementation acceptance still requires the applicable Phase 1 through Phase 6 gates for descriptor, runtime, data owner, frontend, CI evidence, and supply-chain checks.
