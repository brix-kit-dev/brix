package <%= packageName %>.core.entity;

import jakarta.persistence.*;
import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
import lombok.Builder;
import org.hibernate.annotations.Filter;
import org.hibernate.annotations.FilterDef;
import org.hibernate.annotations.ParamDef;

import java.time.LocalDateTime;

/**
 * <%= displayName %> 实体
 * 
 * <p>数据库持久化实体，对应 <%= name %> 表。</p>
 * 
 * <p>【v3.0 多租户支持】</p>
 * <ul>
 *   <li>使用 Hibernate Filter 实现租户数据隔离</li>
 *   <li>tenant_id Field由 TenantContext 自动填充</li>
 *   <li>查询时自动添加租户过滤items件</li>
 * </ul>
 * 
 * @author <%= author %>
 * @date <%= date %>
 * @version <%= version %>
 */
@Data
@Entity
@Builder
@NoArgsConstructor
@AllArgsConstructor
@Table(name = "<%= name.replace(/-/g, '_') %>")
@FilterDef(name = "tenantFilter", parameters = @ParamDef(name = "tenantId", type = String.class))
@Filter(name = "tenantFilter", condition = "tenant_id = :tenantId")
public class <%= classPrefix %>Entity {

    /**
     * Primary key ID
     * <p>使用 UUID 策略生成</p>
     */
    @Id
    @GeneratedValue(strategy = GenerationType.UUID)
    @Column(name = "id", length = 36)
    private String id;

    /**
     * Name
     */
    @Column(name = "name", nullable = false, length = 100)
    private String name;

    /**
     * Description
     */
    @Column(name = "description", length = 500)
    private String description;

    /**
     * Status
     * <p>Possible values：DRAFT, ACTIVE, COMPLETED, CANCELLED</p>
     */
    @Column(name = "status", length = 20)
    private String status;

    /**
     * Tenant ID
     * <p>【v3.0 多租户】不可为空，用于数据隔离</p>
     */
    @Column(name = "tenant_id", nullable = false, length = 36)
    private String tenantId;

    /**
     * Creator ID
     */
    @Column(name = "created_by", length = 36)
    private String createdBy;

    /**
     * Created at
     */
    @Column(name = "created_at", nullable = false, updatable = false)
    private LocalDateTime createdAt;

    /**
     * Updater ID
     */
    @Column(name = "updated_by", length = 36)
    private String updatedBy;

    /**
     * Updated at
     */
    @Column(name = "updated_at")
    private LocalDateTime updatedAt;

    /**
     * 新增时自动设置Created at
     */
    @PrePersist
    protected void onCreate() {
        this.createdAt = LocalDateTime.now();
        this.updatedAt = this.createdAt;
    }

    /**
     * 更新时自动设置Updated at
     */
    @PreUpdate
    protected void onUpdate() {
        this.updatedAt = LocalDateTime.now();
    }
}
