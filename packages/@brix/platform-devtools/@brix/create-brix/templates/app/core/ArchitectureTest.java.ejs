/*
 * Copyright (c) 2024-2026 Brix Framework. All rights reserved.
 *
 * Architecture Constraint Test - Auto-generated by @brix/create-brix scaffolding
 *
 * This file integrates all 13 Brix Architecture Guard red lines:
 *   Rule 1:  Prohibit direct dependency on infrastructure adapters
 *   Rule 2:  Prohibit direct use of Kafka/Redis and other middleware clients
 *   Rule 3:  Prohibit using RestTemplate/WebClient to call other services
 *   Rule 4:  Cross-module communication must go through Capability interfaces
 *   Rule 5:  Prohibit dependency on Spring container-specific APIs
 *   Rule 6:  Configuration must be obtained through ConfigCapability
 *   Rule 7:  Logging standards (Domain/Entity layer prohibits direct use of LoggerFactory)
 *   Rule 8:  Prohibit cross-plugin direct database Repository access
 *   Rule 9:  Prohibit direct coupling between plugins
 *   Rule 10: Prohibit throwing generic exceptions
 *   Rule 11: Prohibit circular dependencies
 *   Rule 12: Security capabilities must be obtained through AuthCapability
 *   Rule 13: Critical events must be published through Transactional Outbox
 *
 * @author <%= author %>
 * @since <%= date %>
 */
package <%= packageName %>;

import com.tngtech.archunit.core.importer.ImportOption;
import com.tngtech.archunit.junit.AnalyzeClasses;
import com.tngtech.archunit.junit.ArchTest;
import com.tngtech.archunit.junit.ArchTests;
import io.brix.architecture.guard.BrixArchitectureRules;

/**
 * <%= displayName %> Architecture Constraint Test
 *
 * <p>Automatically enables all 13 architecture red line guards (42 ArchUnit rules).
 * Any code commit violating these red lines will cause {@code mvn test} to fail.</p>
 *
 * <p>To switch to the relaxed Host layer rule set, replace {@code pluginProfile()} with {@code hostProfile()}.</p>
 *
 * @see BrixArchitectureRules#pluginProfile()
 */
@AnalyzeClasses(
    packages = "<%= packageName %>",
    importOptions = ImportOption.DoNotIncludeTests.class
)
class ArchitectureTest {

    @ArchTest
    static final ArchTests rules = BrixArchitectureRules.pluginProfile();
}
