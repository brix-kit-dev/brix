/**
 * ESLint Flat Config — 由 @brix/create-brix 脚手架自动生成
 *
 * 集成 @brix/eslint-config-architecture 架构守护规则：
 *   - 禁止直接导入 @brix/infra-adapter-*
 *   - 禁止直接使用 axios / node-fetch / got / undici
 *   - 禁止直接访问 localStorage / sessionStorage
 *   - 禁止 console.log（允许 warn / error）
 */
import architectureRules from '@brix/eslint-config-architecture';

export default [
  architectureRules,
  {
    files: ['src/**/*.{ts,tsx}'],
    languageOptions: {
      parserOptions: {
        ecmaVersion: 'latest',
        sourceType: 'module',
        ecmaFeatures: { jsx: true },
      },
    },
  },
  {
    ignores: ['dist/', 'node_modules/', '*.config.*'],
  },
];
