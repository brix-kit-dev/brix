/**
 * ESLint Flat Config — Generated by @brix-sdk/create-brix
 *
 * Integrates @brix-sdk/eslint-config-architecture guard rules:
 *   - Prohibit direct import of @brix/infra-adapter-*
 *   - Prohibit direct use of axios / node-fetch / got / undici
 *   - Prohibit direct access to localStorage / sessionStorage
 *   - Prohibit console.log (allow warn / error)
 */
import architectureRules from '@brix-sdk/eslint-config-architecture';

export default [
  architectureRules,
  {
    files: ['src/**/*.{ts,tsx}'],
    languageOptions: {
      parserOptions: {
        ecmaVersion: 'latest',
        sourceType: 'module',
        ecmaFeatures: { jsx: true },
      },
    },
  },
  {
    ignores: ['dist/', 'node_modules/', '*.config.*'],
  },
];
