/**
 * @file <%= displayName %> Shared module入口
 * @description 多端Total享Type、事件、常量的统一Export
 * @module @brix/<%= name %>-shared
 * @version <%= version %>
 *
 * 【使用方式】
 * ```typescript
 * // Import所有
 * import { <%= classPrefix %>, <%= classPrefix.toUpperCase() %>_EVENTS, API_ENDPOINTS } from '@brix/<%= name %>-shared';
 * 
 * // 按需Import
 * import type { <%= classPrefix %> } from '@brix/<%= name %>-shared/types';
 * import { <%= classPrefix.toUpperCase() %>_EVENTS } from '@brix/<%= name %>-shared/events';
 * import { API_ENDPOINTS } from '@brix/<%= name %>-shared/constants';
 * ```
 */

// TypeExport
export * from './types/index.js';

// 事件Export
export * from './events/index.js';

// 常量Export
export * from './constants/index.js';

// 生成代码Export（如果存在）
// export * from './generated/index.js';
