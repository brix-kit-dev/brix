import { defineConfig } from 'tsup';

/**
 * tsup 构建配置 — <%= displayName %> Shared module
 *
 * @description <%= name %>-shared 多端Total享module的构建配置
 * @version <%= version %>
 *
 * [Design Notes]
 * 本module包含 <%= name %> 插件的多端Total享Type定义：
 * - types/: 业务Type定义（DTO 对应的 TypeScript Type）
 * - events/: 事件Type定义（领域事件Type）
 * - constants/: 常量定义（Status枚举等）
 * - generated/: orval Auto-generated的 API 客户端（来自 OpenAPI）
 *
 * 这些定义被 <%= name %>-ui-web 和 <%= name %>-ui-mobile Total同引用，
 * 确保多端Type一致性。
 *
 * 【架构红线】
 * - Shared module不包含任何运行时逻辑
 * - Shared module不依赖任何框架（React/Vue 等）
 * - Shared module仅包含Type定义和常量
 */
export default defineConfig({
  entry: [
    'src/index.ts',
    'src/types/index.ts',
    'src/events/index.ts',
    'src/constants/index.ts',
    'src/generated/index.ts',
  ],
  format: ['cjs', 'esm'],
  dts: true,
  splitting: false,
  sourcemap: true,
  clean: true,
  treeshake: true,
  external: [
    '@brix/runtime-sdk-api-web',
  ],
});
