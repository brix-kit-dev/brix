package <%= packageName %>.api.event;

import io.runtime.sdk.event.DomainEvent;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.NoArgsConstructor;
import lombok.experimental.SuperBuilder;

/**
 * <%= displayName %>Created Event
 * 
 * <p>When a new<%= displayName %>publishes this domain event when created。</p>
 * 
 * <p>[v3.0 Event Model]</p>
 * <ul>
 *   <li>Extends {@link DomainEvent}，routed uniformly by Runtime Shell</li>
 *   <li>Declared in module-manifest.yaml publishes</li>
 *   <li>moduleDoes not directly operate Kafka/RabbitMQ，but through EventBusCapability publish</li>
 * </ul>
 * 
 * <p>[Usage Example]</p>
 * <pre>{@code
 * // 在 Service 中publish事件
 * var event = <%= classPrefix %>CreatedEvent.builder()
 *     .<%= name.replace(/-/g, '') %>Id(entity.getId())
 *     .<%= name.replace(/-/g, '') %>Name(entity.getName())
 *     .createdBy(context.getAuthContext().getCurrentUserId())
 *     .build();
 * context.getEventBus().publish(event);
 * }</pre>
 * 
 * @author <%= author %>
 * @date <%= date %>
 * @version <%= version %>
 * @see io.runtime.sdk.event.DomainEvent
 */
@Data
@SuperBuilder
@NoArgsConstructor
@EqualsAndHashCode(callSuper = true)
public class <%= classPrefix %>CreatedEvent extends DomainEvent {

    private static final long serialVersionUID = 1L;

    /**
     * 事件Type常量
     */
    public static final String EVENT_TYPE = "<%= classPrefix %>CreatedEvent";

    /**
     * <%= displayName %> ID
     */
    private String <%= name.replace(/-/g, '') %>Id;

    /**
     * <%= displayName %> Name
     */
    private String <%= name.replace(/-/g, '') %>Name;

    /**
     * Creator ID
     */
    private String createdBy;

    /**
     * Tenant ID
     */
    private String tenantId;

    /**
     * 获取事件Type
     * 
     * @return 事件Typecharacters串
     */
    @Override
    public String getEventType() {
        return EVENT_TYPE;
    }

    /**
     * 获取聚合根 ID
     * <p>用于事件路由和分区（保证同一聚合根的事件顺序）</p>
     * 
     * @return 聚合根 ID
     */
    @Override
    public String getAggregateId() {
        return <%= name.replace(/-/g, '') %>Id;
    }
}
