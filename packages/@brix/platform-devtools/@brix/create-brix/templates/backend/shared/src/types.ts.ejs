export interface <%= classPrefix %>Record {
  readonly id: string;
  readonly tenantId: string;
  readonly name: string;
  readonly createdAt: string;
}

export interface Create<%= classPrefix %>Request {
  readonly name: string;
}