import { defineConfig } from 'orval';

export default defineConfig({
  '<%= name %>': {
    input: '../<%= name %>-api/src/main/resources/openapi.json',
    output: {
      target: 'src/generated/api.ts',
      client: 'fetch',
      prettier: true,
    },
  },
});