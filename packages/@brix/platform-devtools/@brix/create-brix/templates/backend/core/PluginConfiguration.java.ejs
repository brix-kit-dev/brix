package <%= packageName %>.config;

import org.springframework.context.annotation.Configuration;

/**
 * <%= displayName %> plugin configuration.
 *
 * <p>Beans must be imported by plugin-owned auto-configuration instead of host package scanning.</p>
 *
 * @author <%= author %>
 */
@Configuration
public class <%= classPrefix %>PluginConfiguration {
}
