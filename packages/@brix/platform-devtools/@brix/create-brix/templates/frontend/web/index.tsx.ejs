<% if (typeof withShared !== 'undefined') { -%>
import './bootstrap';

export { default as App } from './App';
<% } -%>
export { default as <%= classPrefix %>List } from './pages/<%= classPrefix %>List';
export { default as <%= classPrefix %>Detail } from './pages/<%= classPrefix %>Detail';

export const pluginMeta = {
  name: '<%= name %>',
  displayName: '<%= displayName %>',
  version: '<%= sdkVersion %>',
  description: '<%= description %>',
};
