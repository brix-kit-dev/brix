import { defineConfig } from '@rspack/cli';
import { container } from '@rspack/core';
import { getRemoteSharedConfig } from '@brix-sdk/shared-runtime-web/mf-config';

const { ModuleFederationPlugin } = container;

export default defineConfig({
  entry: './src/index.tsx',
  output: {
    publicPath: 'auto',
    clean: true,
  },
  resolve: {
    extensions: ['.tsx', '.ts', '.js'],
  },
  module: {
    rules: [
      {
        test: /\.tsx?$/,
        loader: 'builtin:swc-loader',
        options: {
          jsc: {
            parser: {
              syntax: 'typescript',
              tsx: true,
            },
            transform: {
              react: {
                runtime: 'automatic',
              },
            },
          },
        },
        type: 'javascript/auto',
      },
    ],
  },
  plugins: [
    new ModuleFederationPlugin({
      name: '<%= name.replace(/-/g, "_") %>_web',
      filename: 'remoteEntry.js',
      exposes: {
        './List': './src/exposes/<%= classPrefix %>List.tsx',
        './Detail': './src/exposes/<%= classPrefix %>Detail.tsx',
      },
      shared: getRemoteSharedConfig(),
    }),
  ],
});
