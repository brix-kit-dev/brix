/**
 * <%= displayName %> Mobile - Detail Component
 * 
 * @module @brix/<%= name %>-mobile
 * @generated <%= date %>
 * 
 * React Native Detail Component
 */

import React, { useEffect, useState } from 'react';
import {
  View,
  Text,
  ScrollView,
  StyleSheet,
  ActivityIndicator,
  TouchableOpacity,
} from 'react-native';

/**
 * Detail data interface
 */
interface DetailData {
  id: number;
  title: string;
  description?: string;
  createdAt?: string;
  status?: string;
  [key: string]: unknown;
}

/**
 * Detail component props
 */
interface <%= classPrefix %>DetailProps {
  /**
   * Data ID
   */
  id: number;
  
  /**
   * Back button callback
   */
  onBack?: () => void;
  
  /**
   * Custom styles
   */
  style?: object;
}

/**
 * <%= displayName %> Detail Component
 * 
 * Usage:
 * ```tsx
 * import { <%= classPrefix %>Detail } from '@brix/<%= name %>-mobile';
 * 
 * <{@classPrefix}Detail 
 *   id={123} 
 *   onBack={() => navigation.goBack()} 
 * />
 * ```
 */
export const <%= classPrefix %>Detail: React.FC<<%= classPrefix %>DetailProps> = ({
  id,
  onBack,
  style,
}) => {
  const [data, setData] = useState<DetailData | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  /**
   * Load detail data
   */
  const loadData = async () => {
    try {
      setLoading(true);
      setError(null);
      
      // TODO: Replace with actual API call
      await new Promise(resolve => setTimeout(resolve, 1000));
      setData({
        id,
        title: `Sample Item ${id}`,
        description: 'This is a detailed description text for demonstrating the detail page layout.',
        createdAt: new Date().toISOString(),
        status: 'active',
      });
    } catch (err) {
      setError('Failed to load data, please retry');
      console.error('Failed to load details:', err);
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => {
    loadData();
  }, [id]);

  if (loading) {
    return (
      <View style={[styles.container, styles.center, style]}>
        <ActivityIndicator size="large" color="#1976d2" />
        <Text style={styles.loadingText}>Loading...</Text>
      </View>
    );
  }

  if (error) {
    return (
      <View style={[styles.container, styles.center, style]}>
        <Text style={styles.errorText}>{error}</Text>
        <TouchableOpacity style={styles.retryButton} onPress={loadData}>
          <Text style={styles.retryText}>重试</Text>
        </TouchableOpacity>
      </View>
    );
  }

  if (!data) {
    return (
      <View style={[styles.container, styles.center, style]}>
        <Text style={styles.emptyText}>数据不存在</Text>
        {onBack && (
          <TouchableOpacity style={styles.backButton} onPress={onBack}>
            <Text style={styles.backText}>Back</Text>
          </TouchableOpacity>
        )}
      </View>
    );
  }

  return (
    <ScrollView style={[styles.container, style]}>
      {/* 标题 */}
      <View style={styles.header}>
        <Text style={styles.title}>{data.title}</Text>
        {data.status && (
          <View style={styles.statusBadge}>
            <Text style={styles.statusText}>{data.status}</Text>
          </View>
        )}
      </View>

      {/* 详情内容 */}
      <View style={styles.content}>
        {data.description && (
          <View style={styles.section}>
            <Text style={styles.sectionTitle}>Description</Text>
            <Text style={styles.sectionContent}>{data.description}</Text>
          </View>
        )}

        {data.createdAt && (
          <View style={styles.section}>
            <Text style={styles.sectionTitle}>Created at</Text>
            <Text style={styles.sectionContent}>
              {new Date(data.createdAt).toLocaleString('zh-CN')}
            </Text>
          </View>
        )}
      </View>

      {/* 操作按钮 */}
      <View style={styles.actions}>
        {onBack && (
          <TouchableOpacity style={styles.actionButton} onPress={onBack}>
            <Text style={styles.actionText}>Back</Text>
          </TouchableOpacity>
        )}
      </View>
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f5f5f5',
  },
  center: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  header: {
    backgroundColor: '#fff',
    padding: 16,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  title: {
    fontSize: 20,
    fontWeight: '700',
    color: '#333',
    flex: 1,
  },
  statusBadge: {
    backgroundColor: '#e3f2fd',
    paddingHorizontal: 12,
    paddingVertical: 4,
    borderRadius: 12,
  },
  statusText: {
    fontSize: 12,
    color: '#1976d2',
    fontWeight: '500',
  },
  content: {
    backgroundColor: '#fff',
    marginTop: 12,
    padding: 16,
  },
  section: {
    marginBottom: 16,
  },
  sectionTitle: {
    fontSize: 14,
    fontWeight: '600',
    color: '#666',
    marginBottom: 8,
  },
  sectionContent: {
    fontSize: 16,
    color: '#333',
    lineHeight: 24,
  },
  actions: {
    padding: 16,
    flexDirection: 'row',
    justifyContent: 'center',
  },
  actionButton: {
    backgroundColor: '#1976d2',
    paddingHorizontal: 32,
    paddingVertical: 12,
    borderRadius: 8,
  },
  actionText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '600',
  },
  loadingText: {
    marginTop: 8,
    fontSize: 14,
    color: '#666',
  },
  errorText: {
    fontSize: 14,
    color: '#f44336',
    marginBottom: 16,
  },
  retryButton: {
    backgroundColor: '#1976d2',
    paddingHorizontal: 24,
    paddingVertical: 8,
    borderRadius: 4,
  },
  retryText: {
    color: '#fff',
    fontSize: 14,
  },
  emptyText: {
    fontSize: 14,
    color: '#999',
    marginBottom: 16,
  },
  backButton: {
    backgroundColor: '#e0e0e0',
    paddingHorizontal: 24,
    paddingVertical: 8,
    borderRadius: 4,
  },
  backText: {
    color: '#333',
    fontSize: 14,
  },
});

export default <%= classPrefix %>Detail;
