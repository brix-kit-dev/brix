# <%= displayName %>

<%= description %>

## 项目结构

```
<%= artifactId %>/
├── <%= artifactId %>-api/        # API 契约模块（DTO、事件、请求/响应）
├── <%= artifactId %>-core/       # 核心业务模块（实体、服务、控制器）
├── <%= artifactId %>-ui/         # 前端 UI 模块（React + Module Federation）
├── <%= artifactId %>-app/        # 聚合启动模块（Spring Boot 应用）
├── module-manifest.yaml          # v3.0 模块清单
└── pom.xml                       # 父 POM
```

## v3.0 架构说明

本模块遵循 **Shinwa Platform v3.0 运行壳架构**，核心特点：

### 1. 能力契约（Capability Contract）

模块不直接依赖基础设施，而是通过 `RuntimeContext` 声明式获取能力：

```java
@Service
public class ExampleService {
    
    private final RuntimeContext runtime;
    
    public ExampleService(RuntimeContext runtime) {
        this.runtime = runtime;
    }
    
    public void doSomething() {
        // 获取 HTTP 能力
        HttpCapability http = runtime.getCapability(HttpCapability.class);
        
        // 获取缓存能力
        CacheCapability cache = runtime.getCapability(CacheCapability.class);
        
        // 获取消息能力
        MessagingCapability messaging = runtime.getCapability(MessagingCapability.class);
    }
}
```

### 2. 模块清单（Module Manifest）

`module-manifest.yaml` 定义了模块的元数据和能力需求：

```yaml
module:
  id: <%= name %>
  version: <%= version %>

capabilities:
  required:
    - http
    - cache
  optional:
    - messaging
```

### 3. UI 模块联邦（Module Federation）

UI 模块使用 Rspack Module Federation 实现微前端架构：

- 独立开发和部署
- 运行时动态加载
- 共享依赖管理

## 快速开始

### 环境要求

- JDK 21+
- Maven 3.9+
- Node.js 20+
- pnpm 8+
- Docker & Docker Compose（可选）

### 本地开发

#### 1. 启动基础设施（Docker Compose）

```bash
cd <%= artifactId %>-app
docker-compose up -d postgres redis kafka
```

#### 2. 构建项目

```bash
# 构建所有模块
mvn clean install

# 仅构建后端模块
mvn clean install -pl !<%= artifactId %>-ui
```

#### 3. 启动后端服务

```bash
cd <%= artifactId %>-app
mvn spring-boot:run
```

服务将在 `http://localhost:<%= serverPort %>` 启动。

#### 4. 启动前端 UI

```bash
cd <%= artifactId %>-ui
pnpm install
pnpm dev
```

UI 将在 `http://localhost:<%= uiPort %>` 启动。

### API 文档

启动后访问 Swagger UI：
- http://localhost:<%= serverPort %>/<%= apiPrefix %>/swagger-ui.html

## 模块说明

### API 模块 (`<%= artifactId %>-api`)

定义模块的公共契约：

- **DTO**：数据传输对象
- **Event**：领域事件
- **Request/Response**：API 请求响应对象

### Core 模块 (`<%= artifactId %>-core`)

实现核心业务逻辑：

- **Entity**：JPA 实体
- **Repository**：数据访问层
- **Service**：业务服务层
- **Controller**：REST API 控制器
- **EventHandler**：事件处理器

### UI 模块 (`<%= artifactId %>-ui`)

React 前端应用：

- **pages/**：页面组件
- **hooks/**：业务 Hooks
- **components/**：通用组件

### App 模块 (`<%= artifactId %>-app`)

Spring Boot 启动模块：

- 应用入口类
- 配置文件
- Docker 部署文件

## 配置说明

### 环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `SERVER_PORT` | 服务端口 | `<%= serverPort %>` |
| `SPRING_PROFILES_ACTIVE` | 激活的配置文件 | `dev` |
<% if (features.database) { %>
| `DB_HOST` | 数据库主机 | `localhost` |
| `DB_PORT` | 数据库端口 | `5432` |
| `DB_NAME` | 数据库名称 | `<%= name.replace(/-/g, '_') %>` |
| `DB_USERNAME` | 数据库用户名 | `postgres` |
| `DB_PASSWORD` | 数据库密码 | `postgres` |
<% } %>
<% if (features.cache) { %>
| `REDIS_HOST` | Redis 主机 | `localhost` |
| `REDIS_PORT` | Redis 端口 | `6379` |
<% } %>
<% if (features.messaging) { %>
| `KAFKA_SERVERS` | Kafka 服务器 | `localhost:9092` |
<% } %>

## 部署

### Docker 构建

```bash
# 构建镜像
docker build -t <%= name %>:<%= version %> -f <%= artifactId %>-app/Dockerfile .

# 运行容器
docker run -d -p <%= serverPort %>:<%= serverPort %> <%= name %>:<%= version %>
```

### Kubernetes 部署

参考 `k8s/` 目录下的部署配置（如有）。

## 测试

```bash
# 运行单元测试
mvn test

# 运行集成测试
mvn verify -P integration-test
```

## 贡献

请参考项目根目录的 CONTRIBUTING.md 文件。

## 许可证

版权所有 © Shinwa Technology

---

> 由 `create-shinwa` 脚手架自动生成 | Shinwa Platform v3.0
