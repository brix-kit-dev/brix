# <%= displayName %>

> 生成时间: <%= date %>

## 概述

<%= description %>

## 组装的插件

| 插件 | 版本 |
|---|---|
<% plugins.forEach(plugin => { %>
| <%= plugin.name %> | <%= plugin.version %> |
<% }); %>
<% if (plugins.length === 0) { %>
| （暂无） | - |
<% } %>

## 快速开始

### 本地开发

```bash
# 编译
mvn clean package

# 运行
java -jar target/<%= fullName %>-<%= version %>.jar
```

### Docker 部署

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# View日志
docker-compose logs -f <%= name %>
```

## 配置

### 环境变量

| 变量 | 说明 | Default值 |
|---|---|---|
| `SERVER_PORT` | 服务Port | `<%= port %>` |
| `PLATFORM_HOST_URL` | 基座地址 | `<%= baseUrl %>` |
| `BRIX_SERVICE_API_KEY` | API Key | `platform-service-key` |
| `BRIX_SERVICE_API_SECRET` | API Secret | `platform-service-secret` |
| `DATABASE_URL` | 数据库连接 | `jdbc:postgresql://localhost:5432/brix` |
| `DATABASE_USERNAME` | 数据库用户 | `postgres` |
| `DATABASE_PASSWORD` | 数据库密码 | `postgres` |

### Configuration file

```yaml
# application.yml
brix:
  service:
    name: <%= fullName %>
    base-url: ${PLATFORM_HOST_URL:<%= baseUrl %>}
    heartbeat-interval: <%= heartbeatInterval %>
```

## API 文档

服务启动后访问：`http://localhost:<%= port %>/swagger-ui.html`

## 健康检查

```bash
curl http://localhost:<%= port %>/actuator/health
```

## License

MIT
