<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright 2026 Brix Platform Authors
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->

<!--
============================================================================
BRIX OPEN SOURCE MONOREPO - ROOT AGGREGATOR POM

This is the root Maven POM for the Brix open-source monorepo.
It aggregates all Java modules from the merged repositories.

Architecture Layer: Build Infrastructure
License: Apache 2.0
============================================================================
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.brix</groupId>
    <artifactId>brix-parent</artifactId>
    <version>3.2.0</version>
    <packaging>pom</packaging>

    <name>Brix Framework</name>
    <description>
        Brix Runtime Shell Framework - Open Source Core.
        Provides capability contracts, infrastructure adapters, and platform commons.
    </description>
    <url>https://github.com/brix-framework/brix</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>brix-team</id>
            <name>Brix Platform Team</name>
            <email>team@brix.dev</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/brix-framework/brix.git</connection>
        <developerConnection>scm:git:git@github.com:brix-framework/brix.git</developerConnection>
        <url>https://github.com/brix-framework/brix</url>
    </scm>

    <!--
    ============================================================================
    AGGREGATED MODULES
    
    Each module corresponds to a merged repository's Java content.
    The module paths reference the pom.xml in each package directory.
    ============================================================================
    -->
    <modules>
        <!-- Runtime SDK - Capability contracts and orchestration -->
        <module>packages/@brix/runtime-sdk</module>
        
        <!-- Infrastructure Adapters - Concrete implementations -->
        <module>packages/@brix/infra-adapters</module>
        
        <!-- Platform Commons - Platform-level capabilities -->
        <module>packages/@brix/platform-commons</module>
        
        <!-- Platform DevTools - Development tooling -->
        <module>packages/@brix/platform-devtools</module>
    </modules>

    <properties>
        <!-- Java Version - Aligned with Spring Boot 3.x requirements -->
        <java.version>17</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        
        <!-- Encoding -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        
        <!-- Dependency Versions -->
        <spring-boot.version>3.2.2</spring-boot.version>
        <slf4j.version>2.0.9</slf4j.version>
        <jackson.version>2.16.1</jackson.version>
        <junit.version>5.10.1</junit.version>
        <mockito.version>5.8.0</mockito.version>
        <assertj.version>3.27.7</assertj.version>
        
        <!-- Plugin Versions -->
        <maven-compiler-plugin.version>3.12.1</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.2.3</maven-surefire-plugin.version>
        <maven-source-plugin.version>3.3.0</maven-source-plugin.version>
        <maven-javadoc-plugin.version>3.6.3</maven-javadoc-plugin.version>
        <maven-enforcer-plugin.version>3.4.1</maven-enforcer-plugin.version>
        <jacoco-maven-plugin.version>0.8.11</jacoco-maven-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            
            <!-- Testing -->
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Compiler Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <release>${java.version}</release>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>

                <!-- Surefire Plugin for Tests -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>

                <!-- Source Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- Javadoc Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <configuration>
                        <doclint>none</doclint>
                    </configuration>
                </plugin>

                <!-- Enforcer Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven-enforcer-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>enforce-versions</id>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <configuration>
                                <rules>
                                    <requireMavenVersion>
                                        <version>[3.8.0,)</version>
                                    </requireMavenVersion>
                                    <requireJavaVersion>
                                        <version>[17,)</version>
                                    </requireJavaVersion>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!--
                ============================================================================
                JaCoCo Code Coverage Plugin (Phase 5.3)
                
                Configured in pluginManagement so child modules inherit the setup.
                Activated by the 'coverage' Maven profile to avoid slowing down
                regular builds. CI triggers: mvn verify -Pcoverage
                
                Coverage rules:
                  - Line coverage minimum: 80%
                  - Branch coverage minimum: 70%
                  - Excludes: auto-config classes, DTOs, Application entry points
                ============================================================================
                -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco-maven-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <!--
    ============================================================================
    PROFILES
    
    - release: Enables source/javadoc jar generation for Maven Central
    - ci: Optimized settings for CI/CD pipelines
    ============================================================================
    -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!--
        ================================================================
        Coverage Profile (Phase 5.3)
        
        Activates JaCoCo agent and enforces minimum coverage thresholds.
        
        Usage:
          mvn verify -Pcoverage
        
        CI Integration:
          The code-coverage.yml workflow activates this profile and
          fails the build if line coverage < 80% or branch coverage < 70%.
        
        Exclusions:
          - *AutoConfiguration classes (Spring Boot boilerplate)
          - *Properties classes (configuration POJOs)
          - *Application classes (entry points)
          - **/dto/** (data transfer objects)
          - **/config/** (pure configuration)
        ================================================================
        -->
        <profile>
            <id>coverage</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <executions>
                            <!-- Prepare JaCoCo agent for test execution -->
                            <execution>
                                <id>prepare-agent</id>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                            </execution>
                            <!-- Generate coverage report after tests -->
                            <execution>
                                <id>report</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                            </execution>
                            <!-- Enforce coverage minimums -->
                            <execution>
                                <id>check</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                                <configuration>
                                    <rules>
                                        <rule>
                                            <element>BUNDLE</element>
                                            <limits>
                                                <limit>
                                                    <counter>LINE</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.80</minimum>
                                                </limit>
                                                <limit>
                                                    <counter>BRANCH</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.70</minimum>
                                                </limit>
                                            </limits>
                                        </rule>
                                    </rules>
                                    <excludes>
                                        <!-- Auto-configuration classes (Spring Boot boilerplate) -->
                                        <exclude>**/*AutoConfiguration.*</exclude>
                                        <exclude>**/*AutoConfig.*</exclude>
                                        <!-- Configuration properties (POJOs) -->
                                        <exclude>**/*Properties.*</exclude>
                                        <!-- Application entry points -->
                                        <exclude>**/*Application.*</exclude>
                                        <!-- Data transfer objects -->
                                        <exclude>**/dto/**</exclude>
                                        <!-- Pure configuration classes -->
                                        <exclude>**/config/**</exclude>
                                        <!-- Package info files -->
                                        <exclude>**/package-info.*</exclude>
                                    </excludes>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
